# The stock daemon cannot serve this board's sensor registry: it needs the
# apollo fixes (see hexagonrpcd-apollo). Without them SLPI crash-loops about
# 25 times a minute and takes the rest of the system down with it.
[Service]
ExecStart=
ExecStart=/usr/bin/hexagonrpcd-apollo -f /dev/fastrpc-adsp -d adsp
