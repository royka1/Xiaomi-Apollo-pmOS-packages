# -R is one of the apollo additions: it roots HexagonFS at the device's own
# sensor tree instead of the built-in paths.
[Service]
ExecStart=
ExecStart=/usr/bin/hexagonrpcd-apollo -f /dev/fastrpc-sdsp -d sdsp -s -R /usr/share/qcom/sm8250/Xiaomi/apollo
