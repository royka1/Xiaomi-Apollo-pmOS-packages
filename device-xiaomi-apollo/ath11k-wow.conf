# Keep the QCA6390 firmware alive across s2idle (WoWLAN-mode suspend).
# Without it the non-WoWLAN resume path times out and WiFi is dead after
# every suspend. Needs kernel apollo-7.1.0-r11+.
options ath11k wow_suspend=1
