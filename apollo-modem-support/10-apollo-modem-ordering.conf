[Unit]
# The peripheral manager shim and the ESOC helper both have to be up before
# ModemManager talks to the modem: without the first, secure NV requests fail;
# without the second, the modem never finishes booting in the first place.
After=pm-service-native.service mdm-helper-native.service
Wants=pm-service-native.service
